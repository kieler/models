/**
 * An example of a control flow loop in SCCharts.
 * Be always want the initial state in the first layer.
 * Moveover we want to have a backwards action such that the loop is correctly visualized, even if this causes more backwards edges,
 * more crossings, or ??.
 */
scchart Example01 {

  region {
    initial state A
    go to B
    
    state B
    go to C
    
    state C
    go to D
    
    state D
    go to B
  }
}
